import { Box } from "@mui/material";

export const Divider = () => (
    <Box sx={{
        width: "1px",
        backgroundColor: "#aaa",
        margin: "0 6px",
        height: 32,
    }} >
    </Box>
);